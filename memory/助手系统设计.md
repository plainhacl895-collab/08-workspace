# 团团助手系统设计

更新时间：2026-04-19

## 当前结论

- 当前统一入口：`C:\Users\Huawei\.openclaw\workspace\tools\RUN_TUANTUAN_qidong.cmd`
- 当前实际程序入口：`C:\Users\Huawei\.openclaw\workspace\tools\main.py`
- 当前生产结构是模块化目录：`tools/client`、`tools/property`、`tools/followup`、`tools/experience`、`tools/core`、`tools/utils`
- `tools/tuantuan_cli.py` 与 `tools/tuantuan_cli_zhiling.py` 属于历史单体版本，只保留参考，不再作为当前生产入口

## 设计目标

1. 所有客户、房源、跟进操作都走统一入口。
2. 事实查询和业务判断分离，先查事实，再做建议。
3. 写入动作必须建立在“唯一客户 + 唯一日期/目标”的前提上。
4. 结构治理优先做备份、归档、标注，不允许机器人自行大规模重构。

## 核心数据源

- Excel 主数据：`D:\Unique work form\daily_followup.xlsm`
- 工作区根目录：`C:\Users\Huawei\.openclaw\workspace`
- 当前缓存目录：`runtime/`
- 房源抓取运行目录：`_house_grab_runtime/`

## 当前运行链路

```text
RUN_TUANTUAN_qidong.cmd
  -> main.py
     -> tools/client/*
     -> tools/property/*
     -> tools/followup/*
     -> tools/experience/*
     -> tools/core/*
     -> tools/utils/*
```

## 结构约束

- 不允许机器人自行新增第二入口。
- 不允许机器人因为“更整洁”就批量移动文件。
- 不允许机器人在未经确认的情况下改目录结构、改配置入口、改技能路由。
- 涉及结构整理时，必须先备份，再改动，再验证。

## 当前目录角色

- `tools/`：当前生产代码
- `biaoge_kehu_caozuo_xitong/`：Excel 业务资产和旧版说明
- `workspace/config/`：当前内部配置
- `workspace/docs/`：当前结构文档
- `state/cleanup-2026-04-19/`：本次清理的备份与归档

## 判断优先级

1. `AGENTS.md`
2. `BOOTSTRAP.md`
3. `SOUL.md`
4. `TOOLS.md`
5. `workspace/docs/architecture.md`
6. 本文件

如果其他旧文档与以上内容冲突，以上内容优先。

