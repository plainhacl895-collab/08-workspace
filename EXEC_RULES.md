# EXEC_RULES.md

**生效日期：** 2026-04-04
**维护者：** 团团助手

---

## 执行规则

适用范围：执行脚本、命令、批量整理、结构改造时。

1. 执行前先确认真实路径、目标文件和影响范围。
2. 不在信息不足时擅自修改脚本、配置或数据文件。
3. 批量改动前必须先做备份。
4. 运行失败时先报告原因，不用“猜修复”替代验证。
5. 目录整理优先做归档和标注，少做高风险搬迁。
6. 任何“自动重构”“自动改入口”“自动批量搬文件”都视为高风险行为，必须先征得用户同意。
7. 所有文本读写必须显式指定 UTF-8。
   PowerShell 用 `-Encoding utf8`，Python 用 `encoding="utf-8"`，禁止依赖系统默认编码。
8. Windows 的 `.cmd` / `.bat` 包装脚本默认使用 ASCII 文本。
   除非明确测试过 BOM 与控制台行为，否则不要在批处理里写中文提示语。

---

## ⚠️ 网关操作铁律（2026-04-30 生效，永久有效）

**绝对禁止：**
- ❌ 未经用户明确允许，不得擅自执行 `openclaw gateway stop`
- ❌ 不得擅自执行任何可能导致断连的网关操作
- ❌ 不得在用户未说"OK"之前就关闭网关

**必须遵守：**
- ✅ 需要重启时，优先使用 `openclaw gateway restart`
- ✅ 如果确实需要关闭网关，必须先通知用户，等用户说"OK"才执行
- ✅ 关闭网关前必须说明原因和影响

**原因：** 关闭网关后团团会断线，无法自行重启，只能等用户手动恢复。

